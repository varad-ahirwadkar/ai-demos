# Fraud Detection — model deployment configuration
# Covers: platform setup + Triton model serving only.
# Does NOT include TrustyAI or bias monitoring.
# For bias monitoring use: config-fraud-detection-trustyai.yaml
#
# Usage:
#   rhoai platform setup --config config-fraud-detection.yaml
#   rhoai usecase deploy fraud-detection --config config-fraud-detection.yaml

# Absolute path to the openshift-ai-demos directory inside the cloned repo.
# Example: if you cloned to /home/me/ai-demos, set this to /home/me/ai-demos/openshift-ai-demos
# Note: ~ is not expanded — use the full path.
repo_root: /Users/varad/Desktop/repos/ai-demos/openshift-ai-demos

operator:
  channel: stable-3.5

# DSC components to enable during platform setup.
components:
  - dashboard
  - workbenches
  - kserve

storage:
  class_name: ""   # empty = accept any available ReadWriteOnce class

deployment:
  namespace: test-fraud

  models:
    # Dataset mode: inference_dataset is the single source of truth for the
    # smoke test.
    # inference_request and inference_dataset are mutually exclusive — set one.
    # - name: upi-fraud-detection
    #   model_path:  /Users/varad/Downloads/upi-fraud-detection/model.onnx
    #   config_path: /Users/varad/Downloads/upi-fraud-detection/config.pbtxt
    #   pvc_name:    my-fraud-model-pvc   # optional; defaults to <name>-pvc
    #   pvc_size:    1Gi  
    #   inference_request: /Users/varad/Desktop/repos/ai-demos/openshift-ai-demos/automation/rhoai/usecases/fraud_detection/inputs/upi-fraud-detection.json
    - name: demo-loan-nn-onnx-alpha
      model_path:  /Users/varad/Desktop/automation-framework/model.onnx
      #config_path: /Users/varad/Desktop/repos/ai-demos/config.pbtxt
      pvc_name:    my-fraud-model-pvc   # optional; defaults to <name>-pvc
      pvc_size:    1Gi
      inference_dataset: /Users/varad/Desktop/repos/ai-demos/openshift-ai-demos/automation/rhoai/usecases/fraud_detection/inputs/training/csv/batch_01.csv
