# ocp — generic OpenShift/Kubernetes operations.
# No knowledge of RHOAI, operators, use cases, or business logic.
# Depends only on utils/ and the kubernetes SDK.
# Everything above (platform/, usecases/, cli/) depends on ocp/.
